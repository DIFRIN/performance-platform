# =============================================================================
# Performance Engineering Platform — Secret Template (CNF-03)
# =============================================================================
# TEMPLATE — do NOT commit real credentials to version control.
# Replace <base64-encoded-value> with actual secrets before applying.
#
# Generate base64 values:
#   echo -n "your-password" | base64
#
# Apply:
#   cp secret-template.yaml perf-secret.yaml
#   # edit perf-secret.yaml with real values
#   kubectl apply -f perf-secret.yaml
# =============================================================================

apiVersion: v1
kind: Secret
metadata:
  name: perf-db-secret
  labels:
    app: performance-platform
type: Opaque
data:
  # ---- PostgreSQL credentials ----
  password: <base64-encoded-db-password>
